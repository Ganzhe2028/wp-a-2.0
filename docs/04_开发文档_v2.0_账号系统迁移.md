# OWeek 个人主页系统 · 开发文档 v2.0（账号系统迁移）

面向：opencode（DeepSeek-v4-pro）+ subagents
基准代码：`Ganzhe2028/wp-a` `main`（当前为 v1.0「无账号」实现）

---

## 0. 给 coding agent 的总则

- 先读 `AGENTS.md` 和 `docs/02_开发文档_v1.0`。本文 **覆盖** v1.0 中与身份、登录、收藏相关的部分（见 §1），其余 v1.0 约束全部继续有效。
- `AGENTS.md` 里所有 Critical Gotchas / Pitfalls（Prisma 引擎、两个连接串、presigned 直传、frozen stack、Vercel 部署）**原样适用**。不要因为这次改动违反任何一条。
- **技术栈仍然冻结**：不加任何未列出的库。密码哈希用 Node 内置 `node:crypto`（见 §6），不要装 bcrypt/argon2。
- 按 §11 的顺序逐步实现，每步跑该步验收。可并行的子任务见 §11 标注，按 `AGENTS.md` 规则直接拆给 subagent，不用问。
- 这是一次迁移，不是重写。§2 明确列出「不要动」的部分——别顺手重做它们。

---

## 1. 这一版改什么（v1.0 → v2.0）

| 维度 | v1.0（现状） | v2.0（目标） |
|---|---|---|
| 浏览者身份 | 无身份 | 登录后有身份（账号） |
| 编辑身份 | URL 里的 `editToken`（`/edit/{token}`） | 登录会话（session cookie）→ `/me` 编辑本人 |
| 登录方式 | 无 | 用户名 + 密码，**运营提前批量生成**，学生当天在组长引导下登一次 |
| 会话保持 | 无 | 服务端下发的 httpOnly cookie，14 天，活动期 10+ 天全程不掉（见 §7） |
| 收藏 | localStorage，纯前端 | 服务端，挂在账号上，**单向**（见 §8） |
| 被收藏方 | —— | **永远不通知、不显示次数、不显示是谁**。系统里不存在「谁收藏了我」的任何读路径 |
| Landing `/` | 一个跳 admin 的链接 | 学生登录入口 + 管理员入口 |

> v1.0 §13「不做」里的【服务端账号 / 登录 / 服务端收藏】这三项，本版 **转为要做**。
> 仍然不做：消息通知、评论、点赞、配对算法、学生自助找回密码（密码重置只走「组长 → 开发」人工，见 §9）。

---

## 2. 不要动（保持现状，禁止重做）

以下已稳定，除非本文另有说明，**不改**：

- `/u/[code]`（展板→主页）和 `/loc/[code]`（明信片→展位）的**双页结构**与物理叙事。只给它们加「登录闸 + 收藏改服务端」，渲染逻辑、占位页（hidden / 未 published）保留。
- `LocationCard`（房间/座位）、占位兜底页、`/u` 与 `/loc` 共用同一个 `code`。
- 图片全链路：browser-image-compression → `/api/upload-url` 取 presigned → 直传 R2 → `/api/me/images` 存记录。**不要改成走服务端中转。** `/api/upload-url` 另有用户级内存限流（每人 10 分钟最多 20 次）。
- 校验规则：姓名按 code point ≤ 40，`grade` ≤ 20，`bio` ≤ 80；`avatarUrl` 必须为空或来自 `R2_PUBLIC_BASE_URL`；`published=true` 必须先有 `avatarUrl`；图片 < 4 张且服务端双重校验。
- 运营后台现有能力：QR 批量打印、下架开关、位置编辑、系统设置、以及 **admin 自己的口令登录**（`ADMIN_PASSWORD` → `owk_admin` cookie，`lib/auth.ts` 里的 `createAdminSession/verifyAdminSession`）。admin 登录有 IP 维度内存限流（15 分钟 10 次）。
- Prisma generator 配置（`provider="prisma-client"`、`engineType="client"`、生成产物提交 git）。`binaryTargets` 已移除，改用 `@prisma/adapter-neon` 驱动适配器。

---

## 3. 目录结构（变更标注）

```
app/
  page.tsx                   # ✏️ 改：Landing = 学生登录表单 + 管理员入口
  me/
    page.tsx                 # ➕ 新：本人页（编辑主页 + 我看见了谁），凭 session
  u/[code]/page.tsx          # ✏️ 改：加登录闸（无 session 跳登录）+ 服务端收藏按钮
  loc/[code]/page.tsx        # ✏️ 改：同上
  edit/[token]/page.tsx      # ⛔️ 退役：编辑改到 /me。路由可删（editToken 列保留见 §5）
  me/collection/page.tsx     # ⛔️ 退役：收藏并入 /me 的「我看见了谁」
  admin/page.tsx             # ✏️ 改：导入后可下载帐密 CSV；加「重置密码」
  api/
    auth/login/route.ts      # ➕ 新：POST 用户名+密码 → 下发 session cookie
    auth/logout/route.ts     # ➕ 新：POST 清 cookie
    me/route.ts              # ✏️ 改：身份从 ?token= 改为 session
    me/images/route.ts       # ✏️ 改：身份改为 session
    upload-url/route.ts      # ✏️ 改：身份改为 session
    favorites/route.ts       # ➕ 新：POST 收藏/取消（toggle）
    me/favorites/route.ts    # ➕ 新：GET 我收藏的人列表
    admin/import/route.ts    # ✏️ 改：同时生成 username+password，返回明文一次
    admin/export/route.ts    # ✏️ 改：加 username 列（密码不可事后导出，见 §9）
    admin/reset-password/route.ts  # ➕ 新：POST 重置某人密码，返回新明文一次
    （admin 其余 route 不动）
lib/
  auth.ts                    # ✏️ 改：加 学生 session（create/verify/set/clear）+ 密码哈希
  code.ts                    # ✏️ 改：加 生成可打印密码
  prisma.ts / r2.ts / qr.ts  # 不动
prisma/schema.prisma         # ✏️ 改：Person 加 username/passwordHash；加 Favorite 模型
```

---

## 4. 数据模型变更（prisma/schema.prisma）

Person 加两列；新增 Favorite。其余模型不动。

```prisma
model Person {
  id          String        @id @default(cuid())
  code        String        @unique
  editToken   String        @unique         // 保留列，避免迁移痛；不再是主路径
  username    String        @unique          // ➕ 登录用户名（默认 = code，见 §9）
  passwordHash String?                       // ➕ "salt:hash"（scrypt，见 §6）；建账号后必有值
  englishName String?
  chineseName String?
  grade       String?
  bio         String?
  avatarUrl   String?
  published   Boolean       @default(false)  // 由系统根据 avatarUrl 自动计算，学生无 toggle
  hidden      Boolean       @default(false)
  images      Image[]
  location    LocationCard?
  favoritesGiven    Favorite[] @relation("Favoriter")   // ➕ 我收藏的（要读）
  favoritesReceived Favorite[] @relation("Favoritee")   // ➕ 收藏我的（仅供级联删除，禁止读，见 §8）
  updatedAt   DateTime      @updatedAt
  createdAt   DateTime      @default(now())
}

model Favorite {
  id          String   @id @default(cuid())
  favoriterId String                          // 收藏者（= 当前登录人）
  favoriteeId String                          // 被收藏者
  favoriter   Person   @relation("Favoriter", fields: [favoriterId], references: [id], onDelete: Cascade)
  favoritee   Person   @relation("Favoritee", fields: [favoriteeId], references: [id], onDelete: Cascade)
  createdAt   DateTime @default(now())
  @@unique([favoriterId, favoriteeId])        // 同一对只一条，toggle 用
  @@index([favoriterId])
}
```

> `favoritesReceived` / `Favorite.favoritee` 这条反向关系**只为级联删除存在**。全代码库**不得**出现任何按 `favoriteeId` 反查「谁收藏了我」的查询或接口。这是产品硬决策，不是性能问题。

迁移：`npx prisma migrate dev --name add-accounts-and-favorites`。生产库当前应无正式学生数据（活动在 8 月），可直接迁移；若已有 Person 行，迁移后跑一次 §9 的生成步骤给它们补 username/passwordHash。

---

## 5. 身份与权限（v2.0）

三类身份并存：

1. **学生**：用户名+密码登录 → 服务端签发 `owk_session`（httpOnly JWT，载 `personId`）。所有 `/me`、`/u`、`/loc`、收藏接口都认这个 cookie。无 session 一律跳登录（带 `next` 回跳，见 §10）。
2. **管理员**：维持现状，`ADMIN_PASSWORD` → `owk_admin` cookie，admin 接口校验它。**与学生 session 完全独立**，两套 cookie 互不影响。
3. `editToken`：退役。列保留但主流程不用；`/edit/[token]` 路由可删。
   > ⚠️ **别半删。** `editToken` 列仍是 `String @unique` **非空**约束，所以 **import 必须继续调用 `createUniqueEditToken()` 生成它**，哪怕没人用——否则建账号会撞非空约束、create 直接失败。要区分两件事：① **DB 列**（仍非空，仍要生成）；② **输出层**——`/edit/[token]` 路由、export CSV 里的 `edit` 链接列，这些可以删，跟 DB 列无关。若想彻底清掉这列，**单独**做一次把它改成 `String?` 的迁移，别和本次混在一起。

学生 session（加进 `lib/auth.ts`，复用已有 `jose`，照搬 admin cookie 那套，只是更长效）：

```ts
function getRequiredEnv(name: string): string {
  const value = process.env[name]?.trim();
  if (!value) throw new Error(`Missing required environment variable: ${name}`);
  return value;
}

function getSessionSecret(): Uint8Array {
  return new TextEncoder().encode(getRequiredEnv("SESSION_SECRET"));
}

const SESSION_COOKIE = "owk_session";
const SESSION_MAX_AGE = 60 * 60 * 24 * 14; // 14 天 > 活动 10+ 天

export async function createStudentSession(personId: string): Promise<string> {
  return new SignJWT({ pid: personId, role: "student" })
    .setProtectedHeader({ alg: "HS256" })
    .setIssuedAt()
    .setExpirationTime("14d")
    .sign(getSessionSecret());
}

export async function verifyStudentSession(): Promise<{ personId: string } | null> {
  const token = (await cookies()).get(SESSION_COOKIE)?.value;
  if (!token) return null;
  try {
    const { payload } = await jwtVerify(token, getSessionSecret());
    return { personId: payload.pid as string };
  } catch {
    return null;
  }
}

export function setStudentCookie(token: string) {
  return {
    name: SESSION_COOKIE, value: token,
    httpOnly: true,                                  // ← 关键，见 §7
    secure: process.env.NODE_ENV === "production",
    sameSite: "lax" as const, path: "/",
    maxAge: SESSION_MAX_AGE,
  };
}

export function clearStudentCookie() {
  return { name: SESSION_COOKIE, value: "", httpOnly: true, path: "/", maxAge: 0 };
}
```

新增环境变量：`SESSION_SECRET=`——**至少 32 字节（256 bit）的随机串**（如 `openssl rand -base64 48` 生成）。jose 用它做 HMAC 签名，且 **jose 不会自动校验 key 长度**，太短会埋安全隐患。Vercel 也要填。（现有 admin 用 `ADMIN_PASSWORD` 当 HMAC 密钥，同理建议够长。）`ADMIN_PASSWORD` 和 `SESSION_SECRET` 都必须配置；auth 会懒读取这些变量，缺失时相关 session 操作 fail closed，不允许硬编码 fallback。

---

## 6. 密码哈希（node:crypto，不加库）

栈是冻结的，**不要装 bcrypt/argon2**。用 Node 内置 `scrypt`。放 `lib/auth.ts`：

```ts
import { randomBytes, scryptSync, timingSafeEqual } from "node:crypto";

export function hashPassword(plain: string): string {
  const salt = randomBytes(16);
  const hash = scryptSync(plain, salt, 64);
  return `${salt.toString("hex")}:${hash.toString("hex")}`;
}

export function verifyPassword(plain: string, stored: string): boolean {
  const [saltHex, hashHex] = stored.split(":");
  if (!saltHex || !hashHex) return false;
  const salt = Buffer.from(saltHex, "hex");
  const expected = Buffer.from(hashHex, "hex");
  const actual = scryptSync(plain, salt, expected.length);
  return expected.length === actual.length && timingSafeEqual(expected, actual);
}
```

`scryptSync` 是同步的，登录是低频操作，serverless 下可接受。比较必须用 `timingSafeEqual`，不要用 `===`。

> **运行时**：用到 `node:crypto` 的路由（登录、重置密码、生成账号）必须跑在 **Node runtime**，不要给它们设 `export const runtime = 'edge'`——Edge 上没有 `node:crypto`。App Router 路由处理器默认就是 Node runtime，保持默认即可。（Codex 提到 bcryptjs 也是一个选项，但那是新依赖，`AGENTS.md` 冻结了栈，scrypt 内置且同样安全，故沿用 scrypt；要破例加 bcryptjs 得先跟管 AGENTS.md 的人确认。）

可打印密码生成（`lib/code.ts`，使用独立字母表）：

```ts
const PRINTABLE_PASSWORD_ALPHABET =
  "ABCDEFGHJKLMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz23456789!@#$%^&*-_+=?";
const generatePassword = customAlphabet(PRINTABLE_PASSWORD_ALPHABET, 12);
export function newPlainPassword(): string { return generatePassword(); }
```

12 位、大小写 + 数字 + 少量安全符号，排除易混淆和难手输字符。提供约 69^12 种组合，配合 scrypt hash 可抗离线暴力破解；纸条排版需预留足够宽度。

---

## 7. 会话保持：撑过 10+ 天（关键，必读）

「登一次管 10+ 天」整个压在 cookie 上。两条硬要求：

1. **必须是服务端 `Set-Cookie` 下发的 httpOnly cookie。**
   - iOS Safari 的 ITP 会把**脚本写的 cookie**（`document.cookie`）和 localStorage 砍到约 7 天。服务端下发的 httpOnly cookie **不**受这条 7 天限制约束——这正是选它的原因。
   - 所以：session **只能**走 §5 的 `setStudentCookie`（服务端 `Set-Cookie`）。**禁止**把登录态存进 localStorage、`document.cookie` 或任何前端 JS 写的存储。
   - `owk_admin` 已经是这个模式，照抄即可。
2. **真机验证，别只测当天。** 在真 iPhone Safari 上：登一次 → 隔几天再开 `/u/xxx` → 确认仍是登录态。活动 10+ 天，至少要确认能跨过第 7 天。Safari 的 ITP 细则会随版本变，上线前按当前版本再核一遍，别只信本文。（cookie 的 Max-Age 是上限、不是保证——RFC 6265 允许浏览器因存储压力等提前清；实践中主流平台都撑得住，但正因为不是保证，真机验证是必须项不是可选项。）

> 失败长这样：不是「忘密码」，而是「明明登过却被登出、手边又没纸条」。被登出期间碰的收藏会丢账。所以第 1 条务必做对。

---

## 8. 收藏（服务端，单向，静默）

- 账号之间唯一的交互就是收藏。**单向**：A 收藏 B，只进 A 的列表；B 那边什么都不发生。
- 不通知 B、不给 B 看次数、不给 B 看是谁。系统里不存在「谁收藏了我」的任何接口或页面。
- 存数据库（`Favorite` 表），不存 localStorage——因为现在收藏者是已登录账号，身份已知，存库顺理成章，也不会被 Safari 清掉。
- 自己不能收藏自己（`favoriteeId === session.pid` 直接拒）。
- 收藏按钮初始状态：`/u`、`/loc` 是 server component，渲染时按 session 查这一对是否已存在，把布尔值传给客户端按钮，避免闪烁。

> **不做 localStorage 缓存（明确决定）。** 有 review 会建议「本地缓一份收藏、读得快、能离线、跨设备恢复」。这套设计不需要：页面在登录闸之后才进、本来就联网且要查库渲染主页，收藏顺带一个查询，150 人规模毫无压力；服务端是唯一真相源，换手机登录后收藏自然还在。而加本地缓存就要处理本地与服务端不一致的合并逻辑，是净增复杂度和新的丢账面。要「点一下立刻变心形」用乐观更新即可（先改按钮状态、后台发 POST、失败回滚），不要 localStorage。

---

## 9. 账号预生成与帐密下发（运营侧最关心）

流程：运营提前拿到 100+ 名单 → 开发跑一次生成 → 每人一条 `username + 明文密码` → 印在录取通知书/纸条上 → 学生当天照着输。

实现要点：

- `username` **默认 = 该人的 `code`**（已唯一、已去混淆、本来就要印）。学生纸条上就两样：用户名(=code) + 密码。如运营想要更友好的用户名，可在 import 时传入覆盖。
- import（或一个生成步骤）对每行：建 Person 时生成 `username`、`newPlainPassword()`，存 `passwordHash = hashPassword(明文)`，**把明文放进响应返回**。批量导入必须事务化；任一行失败时整批回滚，不返回半成功账号。
- ⚠️ **明文只在生成那一刻拿得到。** 存库的是 hash，事后无法还原明文。所以：
  - `/api/admin/import` 的响应必须带明文：`created: [{ chineseName, code, username, password }]`。
  - admin 页在导入成功后立刻提供「下载帐密 CSV」，运营当场存下、去印。错过这次就只能重置。
  - `/api/admin/export`（事后导出）**只能**给 `username`，给不了密码——别在这里假装能导密码。
- 忘密码走人工：`POST /api/admin/reset-password { code }` → 生成新明文、更新 hash、**响应返回新明文一次**。组长现场联系开发，开发重置后把新密码报给组长。不做学生自助找回。

---

## 10. 登录闸与回跳

- `/me`、`/u/[code]`、`/loc/[code]`：server component 开头 `verifyStudentSession()`，为空则 `redirect('/?next=' + encodeURIComponent(当前完整路径))`。
- Landing `/`：未登录显示学生登录表单（用户名+密码）+ 角落「管理员入口」链到 `/admin`；已登录则 `redirect('/me')`。
- 登录成功后：若有合法 `next`（站内路径，必须校验以防开放重定向）则跳 `next`，否则跳 `/me`。
- 效果：学生当天先在 `/` 登一次；之后任何一次碰 NFC 弹出的 `/u` 或 `/loc`，cookie 在就直接看；登录态不在（如还没登）就被弹去登录、登完回到刚碰的那个人。**回跳必须做对，否则「先碰了某人再登录」会丢掉那一碰。**

> 备选（不默认做）：若后续希望 `/loc`（纯找座位指路）登录前也能看，可只在点收藏时才要求登录。当前按你的明确要求：两页都登录闸。

---

## 11. 构建顺序与验收（按此推进）

每步做完跑验收再进下一步。标 ⚡ 的步骤彼此独立，可拆 subagent 并行。

> **状态：全部完成（2026-06）。** 以下为构建时的历史顺序，供参考。

1. ✅ **Schema 迁移**：Person 加 `username`/`passwordHash`，新增 `Favorite`，`prisma migrate`。验收：`npx prisma generate && npx tsc --noEmit` 通过。
2. ✅ **lib/auth.ts**：加 `hashPassword/verifyPassword`（§6）+ 学生 session 四件套（§5）。验收：写个临时脚本 hash 一次再 verify 通过；tsc 通过。
3. ✅ **`/api/auth/login` + `/api/auth/logout`**：login 校验 username+password（`verifyPassword`）→ `setStudentCookie`。验收：curl 登录返回 `Set-Cookie: owk_session=...; HttpOnly`；错密码 401。
4. ✅ **Landing `/` 改造**：学生登录表单 + 管理员入口 + 已登录跳 `/me` + `next` 回跳。验收：登录成功落到 `/me` 或 `next`。
5. ✅ **身份切换**：`/api/me`、`/api/me/images`、`/api/upload-url` 从 `?token=` 改为 `verifyStudentSession()`。验收：登录后能改本人资料、能传图（presigned 链路不变）。
6. ✅ **`/me` 页**：编辑本人主页（复用原 `/edit` 的表单与图片组件，身份改 session）+「我看见了谁」区块。验收：能编辑、能发布（头像门槛仍在）。
7. ✅ ⚡ **收藏**：`POST /api/favorites`（toggle，§8）+ `GET /api/me/favorites` + `/u`、`/loc` 的服务端收藏按钮。验收：A 收藏 B 后 `Favorite` 表出现 `(A,B)`；再点取消则删除；B 端无任何变化；自己收藏自己被拒。
8. ✅ **登录闸 + 回跳**：`/me`、`/u`、`/loc` 加 §10 的闸与 `next`。验收：退出登录后开 `/u/某code` → 被弹到 `/` → 登录后回到 `/u/某code`。
9. ✅ ⚡ **运营侧**：import 批量事务生成 username+password 并返回明文、admin 页「下载帐密 CSV」；export 加 username 列；`/api/admin/reset-password`。验收：生成 3 个账号 → 下载到含明文的 CSV → 用其中一个登录成功 → 重置其密码后旧密码失效、新密码可登。
10. ✅ **真机端到端（人工）**：iOS Safari 真机，第 0 天登录 → 跨过第 7 天仍在登录态（§7）；微信内打开的处理（§12）；回跳；同浏览器约束。

---

## 12. 新增 Gotchas（在 AGENTS.md 既有清单之外）

- **session 只能服务端 httpOnly 下发**，不准落 localStorage / `document.cookie`（§7）。这是活动期不掉线的命根子。
- **登录和每次碰 NFC 必须同一个浏览器。** cookie 不跨浏览器。最常见翻车：在**微信内置浏览器**里登的录，碰 NFC 弹出的却是 Safari（两个独立 cookie jar），看起来就像没登过。处理：检测微信 UA（`navigator.userAgent` 含 `MicroMessenger`）时显示引导「请点右上角，在 Safari/默认浏览器中打开」，全程统一用系统默认浏览器。另外两条同方向的坑：① 微信内置浏览器里**别在活动期间登出微信账号**——登出会连带清掉 cookie 和 localStorage；② UC 浏览器内核太旧（约 Chrome 123），SameSite 行为可能有差异。这两条都靠「统一用系统默认浏览器」一条引导规避。
- **明文密码只在生成那一刻可得**，事后只有 hash（§9）。import 必须当场返回并让运营下载，别指望 export 能补。
- **导入名单必须批量事务写入**：不要逐条 `person.create`。100+ 学生会把 Vercel function 时间耗尽，且中途失败会留下半批数据。当前实现先生成短码、editToken、密码，再用 `createMany` 写入 Person 和 LocationCard；失败时返回 JSON 错误给后台。
- **Prisma 不再使用原生引擎**：`schema.prisma` 已改为 `engineType = "client"`，`lib/prisma.ts` 使用 `@prisma/adapter-neon`。若日志再出现 `Prisma Client could not locate the Query Engine`，说明生成产物与 schema 不同步，重新运行 `prisma generate` 并提交。
- **生产 build 当前仍用 webpack**：`package.json` 的 build script 使用 `next build --webpack`（历史遗留）。由于已无引擎二进制需要打包，理论上可改回默认 Turbopack，但切换前需在 Vercel 上验证。
- **收藏单向且静默**：不得新增任何「谁收藏了我」的读路径（§8）。`favoritesReceived` 反向关系只为级联删除。
- **回跳要校验 `next` 是站内路径**，防开放重定向。
- **主页可见性由系统自动决定**：学生上传头像并保存资料后，`/api/me` PATCH 自动把 `published` 设为 `true`；学生端无 toggle。运营仍可用 `hidden` 下架。
- **登录接口已加 rate limiting**：学生登录同一 IP 15 分钟内最多 20 次，同一用户名最多 5 次；admin 登录同一 IP 15 分钟内最多 10 次。当前实现是内存限流，适合短期活动，不是跨实例强安全边界。
- **图片记录必须保存 `/api/upload-url` 返回的真实 `key`**：不要从 `publicUrl` 反推。`/api/upload-url` 每人 10 分钟最多 20 次；`/api/me/images` 会校验 key 属于当前学生、URL 与 key 匹配，并在 Serializable transaction 内做最终数量上限检查。
- **后台人员列表必须显式 select**：`/api/admin/persons` 只返回后台页面需要的字段，禁止把 `passwordHash`、`editToken` 等字段发到浏览器。
- **frozen stack**：密码哈希用 `node:crypto`，不加库。
- **两套 cookie 独立**：`owk_session`（学生）与 `owk_admin`（运营）互不干扰，别合并。
- **Prisma migrate 遇到已有数据时**：加 NOT NULL 列到非空表要手动三步走——`ALTER TABLE ... ADD COLUMN`（nullable）→ `UPDATE ... SET`（回填）→ `ALTER TABLE ... ALTER COLUMN SET NOT NULL` + `CREATE UNIQUE INDEX`。`prisma migrate dev` 此时报错是正常的，不要反复重试。迁移完成后手动创建 migration 文件并插入 `_prisma_migrations` 记录。
- **editToken 列保留生成逻辑，只删路由和输出**：该列是 `String @unique` 非空，`createUniqueEditToken()` 必须留在 `lib/code.ts`，import 路由继续生成。能删的只有：`/edit/[token]` 页面、export 的 edit 链接列、admin 页面的 editToken 展示。彻底清掉这列就单独做一次改 `String?` 的迁移，不要和本次混。

---

## 13. 仍然不做（v2.0 排除）

消息通知、被收藏提醒、评论、点赞、社交链接聚合、配对算法、学生自助找回密码（重置只走 admin 人工）。不要顺手实现。
