# 旧 Person 映射迁移记录（历史）

本次发布明确**不迁移**旧 `Person` 账号、头像、图片或作品。旧数据仅作为数据库备份保留，不参与正式 `User`、Day 1、Day 3、Browse 或 Artwork 页面。

仓库此前曾包含一套人工确认旧 `Person.id` 与正式 `User.accountCode` 对应关系的迁移工具。该工具已随旧运行链路移除，避免在上线前误把旧内容写入新系统。

如活动结束后需要处理旧数据，必须先单独确认数据保留期限、人员归属、内容展示规则和回退方案；不得按姓名、邮箱或相似度自动匹配。
